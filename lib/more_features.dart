import 'package:flutter/material.dart';

class More_Features extends StatelessWidget {
  const More_Features({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('More Features'),
      ),
    );
  }
}
